/* eslint-disable prettier/prettier */
import { View, Text } from "react-native";
import React from "react";
import { COLORS } from "@/constants/theme";


const TripAnalysis = () => {
  return (
    <View>
      <Text style={{ color: COLORS.primary }}>TripAnalysis</Text>
    </View>
  );
};

export default TripAnalysis;
