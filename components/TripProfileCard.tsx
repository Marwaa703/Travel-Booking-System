/* eslint-disable prettier/prettier */

import React from 'react';
import { StyleSheet, View } from 'react-native';

const TripProfileCard = () => {
  return (
    <View>

    </View>
  );
}

const styles = StyleSheet.create({})

export default TripProfileCard;
