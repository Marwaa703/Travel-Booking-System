/* eslint-disable prettier/prettier */
import { View, Text } from "react-native";
import React from "react";
import { colors } from "@/constants/theme";

const Signup = () => {
  return (
    <View>
      <Text style={{ color: colors.primary }}>Sign Up Page</Text>
    </View>
  );
};

export default Signup;